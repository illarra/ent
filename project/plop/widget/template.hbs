{#
Widget variables will be accessible at "root" level: {{{ "{{ foo }}" }}}

There is also a `_config` variable which holds WordPress widget configuration
with the following properties: name, id, description, class, before_widget,
after_widget, before_title, after_title, widget_id, widget_name.
#}
