{{#if (equals type 'container')}}
<!-- Uhmm... actually this doesn't do anything for 'container' types -->
<container/>
{{else}}
<row>
    <column>
        <box>{{ titleCase name }}</box>
    </column>
</row>
{{/if}}
